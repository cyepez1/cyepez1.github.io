# PROJECT — current work

Scrub and reset this file at the start of each page rebuild or sub-project.
Archive completed entries to MEMORY.md, then clear the checklist.

Governing plan: `docs/BUILD-PLAN-2026-REFRESH.md` (W1 rebuild loop).

---

## Just completed: Photos rebuild (W1.1)

Built on `feature/photos-rebuild` — Concept B "Editor's Desk" + lightbox
(decisions archived to MEMORY.md). **Awaiting Cairo's browser preview, then
merge to main.** Also riding that preview: the W0 games-page spot-check.
Push to origin remains HELD.

## Current sub-project: Music rebuild (W1.2)

**Phase:** 5 — Per-page redesigns (rebuild loop: photos → music → misc → index)
**File:** music.html
**Branch:** feature/music-rebuild (created when work starts)
**Treatment (per DESIGN-DIRECTION §7):** hybrid — keep custom components
(waveform, counter tracklist, crate), align to fusion vocabulary; the fake
player takes early-2000s player-skin energy (beveled controls, --gloss, mono
readouts) rendered ink-and-paper; add page accent + denser index/catalog tier.

### Design decisions locked
- (none yet — set at mockup approval)

### Checklist
- [ ] Photos preview + merge done first (G2)
- [ ] Mockup concepts approved (JS moments — player mock interactions —
      decided here, per F6)
- [ ] Scaffold / structure / components commits (one concern each)
- [ ] Docs currency (MEMORY row + this file reset for misc)
- [ ] Browser verify

---

## Rebuild loop remaining
music (this) → misc (corkboard rebuild) → index (hybrid, CRT set-piece — Opus)
Then: W2 blog light pass → W3 11ty+CMS → W4 games pass / links / streaming.
