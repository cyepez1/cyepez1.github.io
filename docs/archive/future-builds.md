# Future builds — deferred, not dead

Two concepts named in the original pre-CLAUDE.md project brief
(`docs/archive/project-instructions-original.md`) that are absent from CLAUDE.md
but **still alive** — both are tracked in `docs/BUILD-PLAN.md` Phase 7 (feature
builds, sequence TBD). Neither is built anywhere on the site today. This file
preserves the concepts so they don't get lost as docs are pruned.

## Garcia Ridge Backyard Naturalist Co.

Bird section, sourced from the original brief:
- Bilingual bird ID cards (English/Spanish)
- Audio (bird calls) — a named stack trigger; needs JS/audio handling
- Sighting log
- Gallery

Asset already staged: `images/house-sparrows-menace.jpg` (kept out of
`images/archive/` during Phase 0 specifically for this build).

## Satirical Chicago civic publication

Sourced from the original brief:
- Its own masthead (a distinct design-system room, like the games section)
- An archive structure for issues/articles

Related archived assets in `images/archive/` (Chicago civic material:
`1938municipalairportexpansion.jpg`, `Archer_ave.classicvideo.jpg`,
`archerbowlingalley.jpg`, `Chicago_Federal_Center.jpg`,
`chicago_type_homes.png`, `movingarcherforward.JPG`) may be relevant
source material when this build starts.

## Status

Deferred to Phase 7 — see `docs/BUILD-PLAN.md`. Not implied as built anywhere.
When either build starts, it gets its own sub-project entry in `docs/PROJECT.md`
per the working method.
